---
description: Open your live cv.ha7ch.com page in the browser.
allowed-tools: Bash(open:*)
---

Use the cv MCP server's `get_resume` tool first to fetch your current handle, then run `open https://cv.ha7ch.com/<handle>` to launch the page.
